#include "main.h"
#include <stdio.h>

/**
 * mul - Multiplies two integers.
 * @a: First integer.
 * @b: Second integer.
 *
 * Return: The result of the multiplication.
 */
int mul(int a, int b)
{
int ans = a * b;
return (ans);
}
