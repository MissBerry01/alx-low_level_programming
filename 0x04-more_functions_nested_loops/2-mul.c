#include "main.h"
#include <stdio.h>

/**
 * mul - a function that multiplies two integers
 * @a : First integer
 * @b : Second integer
 * Return : 0 (success)
 */

int mul(int a, int b)
{
int ans = a * b;
return (ans);
}
