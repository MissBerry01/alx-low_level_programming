#include <stdio.h>
/**
 * main - main point
 * Return : 0 (Success)
 */
int main(void)
{
int i = '0';
while (i <= '9')
{
putchar (i);
if (i != '9')
}
putchar (',');
putchar (',');
{
putchar('\n');
return (0);
}
